export enum UserGender {
  MALE = "male",
  FEMALE = "female",
}

export enum UserRole {
  USER = 'user',
  AUTHOR = 'author',
  ADMIN = "admin",
  LABEL = "label",
  MANAGER = "manager",
}

export enum UserBlocking {
  DELETED = 'deleted',
  BLOCKED = 'blocked',
  COPYRIGHT = 'copyright'
}